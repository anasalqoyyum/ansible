interface:
  display_name: "Blast Radius"
  short_description: "Find and prove risks beyond the current diff"
  default_prompt: "Use $blast-radius to identify what this change could break outside the diff."

policy:
  allow_implicit_invocation: false
